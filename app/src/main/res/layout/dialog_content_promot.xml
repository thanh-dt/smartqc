<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/scr_content"
    android:layout_width="@dimen/width_dialog_content_promotion"
    android:layout_height="@dimen/height_dialog_content_promotion"
    android:background="@android:color/transparent"
    android:orientation="vertical" >

    <ScrollView
        android:id="@+id/src_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginRight="@dimen/margin_5dp"
        android:layout_marginTop="@dimen/margin_5dp"
        android:background="@color/main_color"
        android:padding="@dimen/padding_normal" >

        <RelativeLayout
            android:id="@+id/rl_main"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" >

            <app.com.hss.cooking.magatama.stampcard.TextViewW3
                android:id="@+id/tv_promot"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/app_name"
                android:textColor="@color/white"
                android:textSize="@dimen/size_normal" />
        </RelativeLayout>
    </ScrollView>

    <ImageButton
        android:id="@+id/btn_ok"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_alignParentRight="true"
        android:background="@drawable/btn_close"
        android:contentDescription="@string/app_name"
        android:textColor="@color/white" />

</RelativeLayout>